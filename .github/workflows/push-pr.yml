on:
      push:
            branches:
                  - main
                  - "release/**"
            paths:
                  - 'GitHub Action/**'

      pull_request:
            branches:
                  - main
            paths:
                  - 'lab/**'

jobs:
      build:
            runs-on: ubuntu-latest
            steps:
                  - name: Checkout Code
                    uses: actions/checkout@v4

                  - name: run tests
                    run: |
                          npm test
                          

